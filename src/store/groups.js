const state = {
  groups: [],
};

const getters = {};

const mutations = {
  setGroups: function (state, payload) {
    state.groups = payload;
  },
};
const actions = {};

export { state, getters, mutations, actions };
